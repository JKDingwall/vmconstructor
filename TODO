passwd/group reuse / templates
ramdisk to mount overlay: mkdir /.aufs ; mount /dev/xvdb /.aufs ; mount -t aufs -o noxino,sum,dirs=/.aufs=rw:/root=rr root.union /root ; exit
solidify - hdd
don't install grub until hdd solidify, needs images/block volumes to work on
ovs integration - modify ovs upstart script in netdom
payloads for _update

figlet hello | cowsay -n
