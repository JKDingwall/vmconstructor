# Reference
dist: ubuntu
release: trusty

# Settings
settings:
    onexist: rebuild
    pause: false
    payloads:
        - payloads/ubuntu/repo-bcache
        - payloads/ubuntu/repo-ceph

packages:
    - bcache-tools
    - ceph
    - lvm2
    - mdadm
    - pciutils
    - smartmontools

disks:
    diskdom:
        type: squash
        path: /

data:
    hostname: dmukdd
