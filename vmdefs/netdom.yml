# Reference
dist: ubuntu
release: trusty

# Settings
settings:
    onexist: rebuild
    pause: false

packages:
    - openvswitch-controller
    - openvswitch-switch

disks:
    netdom:
        type: squash
        path: /

data:
