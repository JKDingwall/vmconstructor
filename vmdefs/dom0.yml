# Reference
dist: ubuntu
release: trusty

# Settings
settings:
    onexist: rebuild
    pause: false
    payloads:
      - payloads/ubuntu/repo-bcache

packages:
    - bcache-tools
    - lm-sensors
    - lvm2
    - mdadm
    - smartmontools
    - xen-hypervisor-4.4-amd64

disks:
    dmuknode:
        type: squash
        path: /

data:
    hostname: dmuknode
